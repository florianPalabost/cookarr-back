<?php

declare(strict_types=1);

describe('Http > Controllers > V1 > Auth > RefreshAuthTokenController', function () {
    it('should refresh auth tokens with a valid refresh token', function () {});

    it('should not refresh auth tokens with an invalid refresh token', function () {});
})->group('controllers', 'v1', 'auth');
